{
  "name": "spring-banquet-lucky-maze",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run check:syntax && npm run test && npm run verify",
    "check:syntax": "node --check src/index.js && node --check src/room-core.js && node --check public/app.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "node tools/verify.mjs",
    "load:test:local": "bash tools/run-load-test.sh",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
